maildev: {}

jobs:
  runs:
    build-frontend-usagers:
      with:
        buildArgs:
          NUXT_PUBLIC_ENVIRONMENT: "development"
          NUXT_PUBLIC_SENTRY_ENABLED: "true"
    build-frontend-bo:
      with:
        buildArgs:
          NUXT_PUBLIC_ENVIRONMENT: "development"
          NUXT_PUBLIC_SENTRY_ENABLED: "true"
